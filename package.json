{
  "name": "trmnl-apis",
  "version": "1.0.0",
  "description": "Netlify serverless API service with API key authentication",
  "main": "netlify/functions/api.js",
  "scripts": {
    "dev": "netlify dev",
    "build": "tsc",
    "deploy": "npm run build && netlify deploy --prod",
    "type-check": "tsc --noEmit"
  },
  "keywords": ["netlify", "serverless", "api"],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "serverless-http": "^3.2.0",
    "dotenv": "^16.3.1",
    "@netlify/blobs": "^8.1.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.10.6",
    "netlify-cli": "^17.10.1",
    "typescript": "^5.3.3"
  }
}
